import { vars } from "@/theme/contract.css";
import { style } from "@vanilla-extract/css";

/*
 ** Padding
 */
export const p1 = style({
    padding: vars.spacings[1],
});

export const p2 = style({
    padding: vars.spacings[2],
});

export const p3 = style({
    padding: vars.spacings[3],
});

export const p4 = style({
    padding: vars.spacings[4],
});

/*
 ** Padding Bottom
 */

export const pb1 = style({
    paddingBottom: vars.spacings[1],
});

export const pb2 = style({
    paddingBottom: vars.spacings[2],
});

export const pb3 = style({
    paddingBottom: vars.spacings[3],
});

export const pb4 = style({
    paddingBottom: vars.spacings[4],
});
